You are an operations assistant embedded in an internal platform team's tooling. You plan carefully, call exactly one tool at a time, and never fabricate data. When a tool result is insufficient, you say so and request the next tool.

You have access to the following tools. Respond with a JSON tool call.
{"name": "search_documents", "description": "Search the knowledge base for relevant passages", "parameters": {"query": {"type": "string"},"top_k": {"type": "integer"},"filters": {"type": "object"}}}
{"name": "read_file", "description": "Read a file from the workspace", "parameters": {"path": {"type": "string"},"start_line": {"type": "integer"},"end_line": {"type": "integer"}}}
{"name": "write_file", "description": "Write content to a file in the workspace", "parameters": {"path": {"type": "string"},"content": {"type": "string"},"create_dirs": {"type": "boolean"}}}
{"name": "run_sql", "description": "Execute a read-only SQL query against the analytics warehouse", "parameters": {"query": {"type": "string"},"timeout_seconds": {"type": "integer"}}}
{"name": "send_email", "description": "Send an email on behalf of the user", "parameters": {"to": {"type": "string"},"subject": {"type": "string"},"body": {"type": "string"},"cc": {"type": "array"}}}
{"name": "create_ticket", "description": "Open a ticket in the issue tracker", "parameters": {"project": {"type": "string"},"title": {"type": "string"},"body": {"type": "string"},"labels": {"type": "array"}}}
{"name": "fetch_url", "description": "Fetch and extract the readable content of a web page", "parameters": {"url": {"type": "string"},"render_js": {"type": "boolean"}}}
{"name": "list_calendar", "description": "List calendar events in a time range", "parameters": {"start": {"type": "string"},"end": {"type": "string"},"calendar_id": {"type": "string"}}}

--- retrieved context ---

[doc-001] runbook excerpt
Schema migrations are applied ahead of deployment and must remain backward compatible. Partition keys are derived from the tenant identifier hashed with a stable seed. The ingestion pipeline batches records into windows of up to two thousand events. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Any request exceeding the latency budget is shed rather than queued indefinitely. 
[doc-002] runbook excerpt
The ingestion pipeline batches records into windows of up to two thousand events. Metrics are sampled once per second and aggregated into one-minute buckets downstream. The ingestion pipeline batches records into windows of up to two thousand events. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Partition keys are derived from the tenant identifier hashed with a stable seed. Partition keys are derived from the tenant identifier hashed with a stable seed. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Metrics are sampled once per second and aggregated into one-minute buckets downstream. 
[doc-003] runbook excerpt
Partition keys are derived from the tenant identifier hashed with a stable seed. The ingestion pipeline batches records into windows of up to two thousand events. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Metrics are sampled once per second and aggregated into one-minute buckets downstream. 
[doc-004] runbook excerpt
The ingestion pipeline batches records into windows of up to two thousand events. Partition keys are derived from the tenant identifier hashed with a stable seed. The ingestion pipeline batches records into windows of up to two thousand events. Metrics are sampled once per second and aggregated into one-minute buckets downstream. The ingestion pipeline batches records into windows of up to two thousand events. Schema migrations are applied ahead of deployment and must remain backward compatible. Access to the warehouse is mediated by short-lived credentials issued per session. Partition keys are derived from the tenant identifier hashed with a stable seed. Schema migrations are applied ahead of deployment and must remain backward compatible. 
[doc-005] runbook excerpt
Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Access to the warehouse is mediated by short-lived credentials issued per session. Schema migrations are applied ahead of deployment and must remain backward compatible. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Any request exceeding the latency budget is shed rather than queued indefinitely. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. 
[doc-006] runbook excerpt
The ingestion pipeline batches records into windows of up to two thousand events. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Cold storage tiers are reconciled nightly against the primary index for drift. Partition keys are derived from the tenant identifier hashed with a stable seed. Any request exceeding the latency budget is shed rather than queued indefinitely. Cold storage tiers are reconciled nightly against the primary index for drift. Cold storage tiers are reconciled nightly against the primary index for drift. Any request exceeding the latency budget is shed rather than queued indefinitely. 
[doc-007] runbook excerpt
Metrics are sampled once per second and aggregated into one-minute buckets downstream. Schema migrations are applied ahead of deployment and must remain backward compatible. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Access to the warehouse is mediated by short-lived credentials issued per session. Cold storage tiers are reconciled nightly against the primary index for drift. 
[doc-008] runbook excerpt
Cold storage tiers are reconciled nightly against the primary index for drift. Access to the warehouse is mediated by short-lived credentials issued per session. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Partition keys are derived from the tenant identifier hashed with a stable seed. Schema migrations are applied ahead of deployment and must remain backward compatible. 
[doc-009] runbook excerpt
Schema migrations are applied ahead of deployment and must remain backward compatible. Cold storage tiers are reconciled nightly against the primary index for drift. Partition keys are derived from the tenant identifier hashed with a stable seed. The ingestion pipeline batches records into windows of up to two thousand events. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Any request exceeding the latency budget is shed rather than queued indefinitely. 
[doc-010] runbook excerpt
Any request exceeding the latency budget is shed rather than queued indefinitely. Cold storage tiers are reconciled nightly against the primary index for drift. Cold storage tiers are reconciled nightly against the primary index for drift. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Access to the warehouse is mediated by short-lived credentials issued per session. 
[doc-011] runbook excerpt
Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. The ingestion pipeline batches records into windows of up to two thousand events. Access to the warehouse is mediated by short-lived credentials issued per session. Cold storage tiers are reconciled nightly against the primary index for drift. Access to the warehouse is mediated by short-lived credentials issued per session. Partition keys are derived from the tenant identifier hashed with a stable seed. Any request exceeding the latency budget is shed rather than queued indefinitely. 
[doc-012] runbook excerpt
Cold storage tiers are reconciled nightly against the primary index for drift. Any request exceeding the latency budget is shed rather than queued indefinitely. Schema migrations are applied ahead of deployment and must remain backward compatible. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. 
[doc-013] runbook excerpt
The ingestion pipeline batches records into windows of up to two thousand events. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Access to the warehouse is mediated by short-lived credentials issued per session. Schema migrations are applied ahead of deployment and must remain backward compatible. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Partition keys are derived from the tenant identifier hashed with a stable seed. Partition keys are derived from the tenant identifier hashed with a stable seed. 
[doc-014] runbook excerpt
Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Schema migrations are applied ahead of deployment and must remain backward compatible. Cold storage tiers are reconciled nightly against the primary index for drift. Partition keys are derived from the tenant identifier hashed with a stable seed. Access to the warehouse is mediated by short-lived credentials issued per session. Schema migrations are applied ahead of deployment and must remain backward compatible. Partition keys are derived from the tenant identifier hashed with a stable seed. 
[doc-015] runbook excerpt
Access to the warehouse is mediated by short-lived credentials issued per session. Partition keys are derived from the tenant identifier hashed with a stable seed. Any request exceeding the latency budget is shed rather than queued indefinitely. Partition keys are derived from the tenant identifier hashed with a stable seed. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Schema migrations are applied ahead of deployment and must remain backward compatible. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Schema migrations are applied ahead of deployment and must remain backward compatible. 
[doc-016] runbook excerpt
Metrics are sampled once per second and aggregated into one-minute buckets downstream. Metrics are sampled once per second and aggregated into one-minute buckets downstream. The ingestion pipeline batches records into windows of up to two thousand events. Cold storage tiers are reconciled nightly against the primary index for drift. Schema migrations are applied ahead of deployment and must remain backward compatible. 
[doc-017] runbook excerpt
Access to the warehouse is mediated by short-lived credentials issued per session. The ingestion pipeline batches records into windows of up to two thousand events. Schema migrations are applied ahead of deployment and must remain backward compatible. Partition keys are derived from the tenant identifier hashed with a stable seed. Any request exceeding the latency budget is shed rather than queued indefinitely. Any request exceeding the latency budget is shed rather than queued indefinitely. 
[doc-018] runbook excerpt
The ingestion pipeline batches records into windows of up to two thousand events. Cold storage tiers are reconciled nightly against the primary index for drift. Partition keys are derived from the tenant identifier hashed with a stable seed. Partition keys are derived from the tenant identifier hashed with a stable seed. Partition keys are derived from the tenant identifier hashed with a stable seed. 
[doc-019] runbook excerpt
Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Cold storage tiers are reconciled nightly against the primary index for drift. Partition keys are derived from the tenant identifier hashed with a stable seed. The ingestion pipeline batches records into windows of up to two thousand events. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Metrics are sampled once per second and aggregated into one-minute buckets downstream. 
[doc-020] runbook excerpt
Schema migrations are applied ahead of deployment and must remain backward compatible. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Any request exceeding the latency budget is shed rather than queued indefinitely. The ingestion pipeline batches records into windows of up to two thousand events. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. The ingestion pipeline batches records into windows of up to two thousand events. Schema migrations are applied ahead of deployment and must remain backward compatible. 
[doc-021] runbook excerpt
Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Any request exceeding the latency budget is shed rather than queued indefinitely. The ingestion pipeline batches records into windows of up to two thousand events. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Partition keys are derived from the tenant identifier hashed with a stable seed. Schema migrations are applied ahead of deployment and must remain backward compatible. Access to the warehouse is mediated by short-lived credentials issued per session. 
[doc-022] runbook excerpt
Any request exceeding the latency budget is shed rather than queued indefinitely. Cold storage tiers are reconciled nightly against the primary index for drift. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Cold storage tiers are reconciled nightly against the primary index for drift. Cold storage tiers are reconciled nightly against the primary index for drift. 
[doc-023] runbook excerpt
Cold storage tiers are reconciled nightly against the primary index for drift. Access to the warehouse is mediated by short-lived credentials issued per session. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Schema migrations are applied ahead of deployment and must remain backward compatible. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Any request exceeding the latency budget is shed rather than queued indefinitely. Access to the warehouse is mediated by short-lived credentials issued per session. 
[doc-024] runbook excerpt
Schema migrations are applied ahead of deployment and must remain backward compatible. The ingestion pipeline batches records into windows of up to two thousand events. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Any request exceeding the latency budget is shed rather than queued indefinitely. Schema migrations are applied ahead of deployment and must remain backward compatible. The ingestion pipeline batches records into windows of up to two thousand events. Access to the warehouse is mediated by short-lived credentials issued per session. 
[doc-025] runbook excerpt
Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Access to the warehouse is mediated by short-lived credentials issued per session. Any request exceeding the latency budget is shed rather than queued indefinitely. Schema migrations are applied ahead of deployment and must remain backward compatible. Any request exceeding the latency budget is shed rather than queued indefinitely. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Any request exceeding the latency budget is shed rather than queued indefinitely. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Metrics are sampled once per second and aggregated into one-minute buckets downstream. 
[doc-026] runbook excerpt
Partition keys are derived from the tenant identifier hashed with a stable seed. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Cold storage tiers are reconciled nightly against the primary index for drift. Any request exceeding the latency budget is shed rather than queued indefinitely. 
[doc-027] runbook excerpt
The ingestion pipeline batches records into windows of up to two thousand events. The ingestion pipeline batches records into windows of up to two thousand events. Access to the warehouse is mediated by short-lived credentials issued per session. Cold storage tiers are reconciled nightly against the primary index for drift. Access to the warehouse is mediated by short-lived credentials issued per session. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Any request exceeding the latency budget is shed rather than queued indefinitely. Cold storage tiers are reconciled nightly against the primary index for drift. Any request exceeding the latency budget is shed rather than queued indefinitely. 
[doc-028] runbook excerpt
Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Cold storage tiers are reconciled nightly against the primary index for drift. Metrics are sampled once per second and aggregated into one-minute buckets downstream. 
[doc-029] runbook excerpt
Metrics are sampled once per second and aggregated into one-minute buckets downstream. Cold storage tiers are reconciled nightly against the primary index for drift. The ingestion pipeline batches records into windows of up to two thousand events. Cold storage tiers are reconciled nightly against the primary index for drift. Any request exceeding the latency budget is shed rather than queued indefinitely. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. 
[doc-030] runbook excerpt
Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Partition keys are derived from the tenant identifier hashed with a stable seed. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Cold storage tiers are reconciled nightly against the primary index for drift. Schema migrations are applied ahead of deployment and must remain backward compatible. Partition keys are derived from the tenant identifier hashed with a stable seed. Any request exceeding the latency budget is shed rather than queued indefinitely. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Partition keys are derived from the tenant identifier hashed with a stable seed. 
[doc-031] runbook excerpt
Partition keys are derived from the tenant identifier hashed with a stable seed. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Schema migrations are applied ahead of deployment and must remain backward compatible. Schema migrations are applied ahead of deployment and must remain backward compatible. Schema migrations are applied ahead of deployment and must remain backward compatible. The ingestion pipeline batches records into windows of up to two thousand events. Schema migrations are applied ahead of deployment and must remain backward compatible. 
[doc-032] runbook excerpt
Cold storage tiers are reconciled nightly against the primary index for drift. Schema migrations are applied ahead of deployment and must remain backward compatible. Cold storage tiers are reconciled nightly against the primary index for drift. Any request exceeding the latency budget is shed rather than queued indefinitely. Schema migrations are applied ahead of deployment and must remain backward compatible. Schema migrations are applied ahead of deployment and must remain backward compatible. The ingestion pipeline batches records into windows of up to two thousand events. The ingestion pipeline batches records into windows of up to two thousand events. 
[doc-033] runbook excerpt
Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Schema migrations are applied ahead of deployment and must remain backward compatible. Partition keys are derived from the tenant identifier hashed with a stable seed. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Metrics are sampled once per second and aggregated into one-minute buckets downstream. The ingestion pipeline batches records into windows of up to two thousand events. Access to the warehouse is mediated by short-lived credentials issued per session. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Access to the warehouse is mediated by short-lived credentials issued per session. 
[doc-034] runbook excerpt
Metrics are sampled once per second and aggregated into one-minute buckets downstream. Any request exceeding the latency budget is shed rather than queued indefinitely. Access to the warehouse is mediated by short-lived credentials issued per session. Partition keys are derived from the tenant identifier hashed with a stable seed. Schema migrations are applied ahead of deployment and must remain backward compatible. The ingestion pipeline batches records into windows of up to two thousand events. Any request exceeding the latency budget is shed rather than queued indefinitely. Cold storage tiers are reconciled nightly against the primary index for drift. 
[doc-035] runbook excerpt
Partition keys are derived from the tenant identifier hashed with a stable seed. Schema migrations are applied ahead of deployment and must remain backward compatible. Schema migrations are applied ahead of deployment and must remain backward compatible. The ingestion pipeline batches records into windows of up to two thousand events. Cold storage tiers are reconciled nightly against the primary index for drift. Schema migrations are applied ahead of deployment and must remain backward compatible. The ingestion pipeline batches records into windows of up to two thousand events. Schema migrations are applied ahead of deployment and must remain backward compatible. Schema migrations are applied ahead of deployment and must remain backward compatible. 
[doc-036] runbook excerpt
Cold storage tiers are reconciled nightly against the primary index for drift. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. The ingestion pipeline batches records into windows of up to two thousand events. Any request exceeding the latency budget is shed rather than queued indefinitely. Cold storage tiers are reconciled nightly against the primary index for drift. 
[doc-037] runbook excerpt
The ingestion pipeline batches records into windows of up to two thousand events. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Access to the warehouse is mediated by short-lived credentials issued per session. 
[doc-038] runbook excerpt
Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Cold storage tiers are reconciled nightly against the primary index for drift. The ingestion pipeline batches records into windows of up to two thousand events. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. 
[doc-039] runbook excerpt
Any request exceeding the latency budget is shed rather than queued indefinitely. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Access to the warehouse is mediated by short-lived credentials issued per session. Cold storage tiers are reconciled nightly against the primary index for drift. Cold storage tiers are reconciled nightly against the primary index for drift. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Access to the warehouse is mediated by short-lived credentials issued per session. 
[doc-040] runbook excerpt
Metrics are sampled once per second and aggregated into one-minute buckets downstream. Cold storage tiers are reconciled nightly against the primary index for drift. Schema migrations are applied ahead of deployment and must remain backward compatible. Partition keys are derived from the tenant identifier hashed with a stable seed. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Partition keys are derived from the tenant identifier hashed with a stable seed. Cold storage tiers are reconciled nightly against the primary index for drift. Any request exceeding the latency budget is shed rather than queued indefinitely. 
[doc-041] runbook excerpt
Metrics are sampled once per second and aggregated into one-minute buckets downstream. Partition keys are derived from the tenant identifier hashed with a stable seed. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Metrics are sampled once per second and aggregated into one-minute buckets downstream. 
[doc-042] runbook excerpt
Access to the warehouse is mediated by short-lived credentials issued per session. Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Schema migrations are applied ahead of deployment and must remain backward compatible. Any request exceeding the latency budget is shed rather than queued indefinitely. Schema migrations are applied ahead of deployment and must remain backward compatible. Access to the warehouse is mediated by short-lived credentials issued per session. Schema migrations are applied ahead of deployment and must remain backward compatible. Cold storage tiers are reconciled nightly against the primary index for drift. Metrics are sampled once per second and aggregated into one-minute buckets downstream. 
[doc-043] runbook excerpt
Retries use exponential backoff with a jitter factor drawn uniformly from zero to one. Partition keys are derived from the tenant identifier hashed with a stable seed. Cold storage tiers are reconciled nightly against the primary index for drift. Schema migrations are applied ahead of deployment and must remain backward compatible. Metrics are sampled once per second and aggregated into one-minute buckets downstream. Schema migrations are applied ahead of deployment and must remain backward compatible. Partition keys are derived from the tenant identifier hashed with a stable seed. Partition keys are derived from the tenant identifier hashed with a stable seed. Any request exceeding the latency budget is shed rather than queued indefinitely. 

--- conversation ---
user: The nightly reconciliation job failed again. Find out which partition drifted and open a ticket with the details.
assistant: I'll query the warehouse for the reconciliation results first.
tool_result: {"rows": [{"partition": "tenant_a4f2", "expected": 88214, "actual": 88190}]}
user: Good. Now open the ticket.

Respond with the next tool call as JSON only.
